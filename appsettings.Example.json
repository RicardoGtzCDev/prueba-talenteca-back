{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "JWT_SECRET_KEY": "secret key to generate JWT's",
  "ConnectionStrings": {
    "SQL_AZUREDB": "SQL AzureDB connection string"
  }
}
