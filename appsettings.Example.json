{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "JWT_SECRET_KEY": "secret key to generate JWT's",
  "AZURE_DB_SQL": "SQL AzureDB connection string"
}
